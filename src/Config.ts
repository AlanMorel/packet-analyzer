const app = {
    name: "Packet Analyzer",
    slug: "packet-analyzer",
    domain: "mapleme.me",
    subpath: "/packet-analyzer",
    url: "https://mapleme.me/packet-analyzer"
};

const Config = {
    app
};

export default Config;
